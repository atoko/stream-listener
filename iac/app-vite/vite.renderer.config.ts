import { defineConfig } from 'vite';

export default defineConfig({
    // build: {
    //     rollupOptions: {
    //         external: ['react-dom', 'react-dom/client', 'react-router-dom'],
    //     }
    // },
    // plugins: [
    //     // pages({
    //     //     pagesDir: join(__dirname, 'app'),
    //     // })
    // ],
});
